Mudar todas as ocorrências do texto "Nova conversa" para "Novo contrato" no componente `src/components/shell/ClaudeSidebar.tsx`:

- Botão principal da sidebar (label + tooltip)
- Fallback de título quando o thread não tem título ainda

Nenhuma outra alteração será feita.